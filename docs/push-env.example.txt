# Web Push (VAPID) env vars for local/dev
#
# Do NOT commit real private keys to git.
# Put these values into your local environment or your hosting provider.
#
# Required for:
#   - "Отримати push-нагадування" on booking confirmation
#   - Manual "Нагадати (push)" button in appointments
#   - Automatic push reminders via cron (GET /api/cron/send-appointment-reminders)
#
# На телефоні: Android — працює в браузері; iPhone — потрібно спочатку додати сторінку
# на початок екрану (Поделитися → На екран Домівки), iOS 16.4+.

NEXT_PUBLIC_VAPID_PUBLIC_KEY=<your_public_key>
VAPID_PRIVATE_KEY=<your_private_key>
VAPID_SUBJECT=mailto:support@your-domain.com

