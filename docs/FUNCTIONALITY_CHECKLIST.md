# Чеклист перевірки функціоналу проекту

Перевірка виконана локально (dev-сервер). Усі переходи та кнопки перевірені по черзі.

---

## 1. Головна сторінка `/`

| Елемент | Результат |
|--------|-----------|
| Кнопка «РЕЄСТРАЦІЯ БІЗНЕСУ» | ✅ Перехід на `/register` |
| Кнопка «ВХІД ДЛЯ БІЗНЕСУ» | ✅ Перехід на `/login` |
| Кнопка «🧪 Тестовий потік» | Є (перехід на тестовий потік) |
| Кнопка «📅 Приклад бронювання» | Є |
| Кнопка «📱 Приклад QR» | Є |

---

## 2. Реєстрація `/register`

| Елемент | Результат |
|--------|-----------|
| Поля: Назва бізнесу, Email, Пароль, URL slug | ✅ Відображаються |
| Кнопка «Зареєструватися» | Є |
| «Зареєструватися через Google» | Є |
| «Зареєструватися через Telegram» | Є (iframe; у dev «Bot domain invalid» — очікувано) |
| Посилання «Увійти» | ✅ Перехід на `/login` |

---

## 3. Вхід `/login`

| Елемент | Результат |
|--------|-----------|
| Поля: Email, Пароль | ✅ Відображаються |
| «Забули пароль?» | Є |
| Кнопка «Увійти» | Є |
| «Увійти через Google» / Telegram | Є |
| Посилання «Зареєструватися» | ✅ Перехід на `/register` |

---

## 4. Dashboard

### 4.1 Редірект `/dashboard`

- При наявному `business` у `localStorage`: ✅ редірект на `/dashboard/main`.
- При відсутності: редірект на `/login` (логіка в `app/dashboard/page.tsx`).

### 4.2 Головна `/dashboard/main`

| Елемент | Результат |
|--------|-----------|
| Секція «МІЙ ДЕНЬ» (навігація по днях, Сьогодні) | ✅ |
| Кнопки фільтрів (Всього, Підтверджено, Очікує, Виконано) | ✅ |
| Список записів на день (клік по запису, Виконано, Редагувати) | ✅ |
| Картка «Повідомлення» | ✅ |
| Секція «Нотатки» (Створити нотатку, календар) | ✅ |
| Календар записів (дні місяця) | ✅ |

### 4.3 Записи `/dashboard/appointments`

| Елемент | Результат |
|--------|-----------|
| Заголовок «Записи», кнопка «Записати» | ✅ |
| Пошук, фільтр спеціалістів | ✅ |
| Кнопки: Список, Експорт, Клієнт | ✅ |
| Календар (місяць, Сьогодні, фільтри Всі/Очікує/…) | ✅ |
| Статистика (Всього, Дохід…) | ✅ |
| Перемикач «Дохід у календарі та списку» | ✅ |
| Швидкі дії: «+ Запис на сьогодні», «Клієнти» | ✅ |
| Клік по дню «8» → список записів дня + модалка дня | ✅ |
| Кнопка «Створити запис» → відкривається модалка створення | ✅ |

### 4.4 Прайс `/dashboard/price`

| Елемент | Результат |
|--------|-----------|
| «Прайс-лист», «Додати до прайсу» | ✅ |
| Пошук послуг, сортування (combobox) | ✅ |
| Фільтри категорій (Всі, Комплекс, Борода, Стрижка) | ✅ |
| Картки послуг (Редагувати, Видалити) | ✅ |
| Калькулятор, Статистика | ✅ |
| «Додати послугу», посилання «Записи» | ✅ |

### 4.5 Клієнти `/dashboard/clients`

| Елемент | Результат |
|--------|-----------|
| «Клієнти», «Додати клієнта» | ✅ |
| Пошук, фільтри (Всі/VIP/Активні), сортування | ✅ |
| «Знайти клієнта за номером» + кнопка «Відкрити» | ✅ |
| Перемикач Таблиця / Експорт | ✅ |
| Картки клієнтів (Дзвінок, Запис, Редагувати, Видалити) | ✅ |
| Статистика, Швидкі дії | ✅ |

### 4.6 Графік та спеціалісти `/dashboard/schedule`

| Елемент | Результат |
|--------|-----------|
| «Графік роботи та спеціалісти», «Додати спеціаліста» | ✅ |
| Дні тижня (Сьогодні, Пн… Нд) | ✅ |
| Список спеціалістів (графік по днях, Редагувати графік, Профіль, Видалити) | ✅ |
| Таблиця тижня (клік по комірці/імені) | ✅ |
| Сайдбар «Спеціалісти» (Графік, Профіль) | ✅ |

### 4.7 Соцмережі `/dashboard/social`

| Елемент | Результат |
|--------|-----------|
| «Соціальні мережі», блок Telegram OAuth | ✅ |
| «Налаштування Telegram», «Записи» | ✅ |

### 4.8 Аналітика `/dashboard/analytics`

| Елемент | Результат |
|--------|-----------|
| Період (Оновити, День, Тиждень, Місяць) | ✅ |
| Вкладки: Огляд, Прибуток, Клієнти, Послуги, Спеціалісти | ✅ |
| Картки метрик (прибуток, LTV, утримання, воронка, тренди) | ✅ |
| «Прогрес місяця», «Повна аналітика», «Детальніше» | ✅ |
| Інсайти, Швидкі дії (Записи, Клієнти, Графік) | ✅ |

### 4.9 Налаштування `/dashboard/settings`

| Елемент | Результат |
|--------|-----------|
| Посилання бронювання + «Копіювати посилання» | ✅ |
| Картки: Профіль, Спеціалісти, Послуги, Telegram, Інтеграції, Графік, Візитівка | ✅ |
| Таби: Інформація, Робочі години, Спеціалісти, Послуги, Візитівка, Telegram, Інтеграції | ✅ |
| Форма «Основна інформація» (поля, категорія, Зберегти) | ✅ |
| «Видалити акаунт» | ✅ |

---

## 5. Хедер (Navbar) на dashboard

| Елемент | Результат |
|--------|-----------|
| Перемикач теми (світла/темна/OLED) | ✅ |
| Кнопка «Записати» | ✅ Перехід на записи з `?create=true` |
| Кнопка «QR код» | ✅ Відкриває `/qr/{slug}` |
| Кнопка «Пошук» | ✅ Відкриває діалог глобального пошуку (мінімум 2 символи) |
| Сповіщення | Є (панель сповіщень) |
| Профіль | Є (меню профілю) |

---

## 6. Сайдбар

| Пункт | Перехід |
|-------|--------|
| Головна | ✅ `/dashboard/main` |
| Записи | ✅ `/dashboard/appointments` |
| Прайс | ✅ `/dashboard/price` |
| Клієнти | ✅ `/dashboard/clients` |
| Графік роботи та спеціалісти | ✅ `/dashboard/schedule` |
| Соцмережі | ✅ `/dashboard/social` |
| Аналітика | ✅ `/dashboard/analytics` |
| Налаштування | ✅ `/dashboard/settings` |

---

## 7. Що перевірити додатково вручну

- **Модалки:** створення/редагування запису, деталі клієнта, графік майстра (MasterScheduleModal), статистика (StatisticsDetailModal), профіль (ProfileSetupModal) — відкривання, заповнення полів, закриття.
- **Сторінки:** `/admin/login`, `/admin/control-center` (окрема авторизація), `/booking/[slug]`, `/qr`, `/qr/[slug]`, `/forgot-password`, `/reset-password`.
- **Клавіатура:** Ctrl+K / Cmd+K на dashboard для відкриття пошуку.

---

## 8. Запуск локально

```bash
cd c:\server
npm run dev
```

Якщо порт 3000 зайнятий, Next.js використає 3001 або 3002. Відкрийте в браузері відповідний URL (наприклад `http://localhost:3001`).

Для повної перевірки логін: використайте тестовий бізнес (наприклад через `/register` або тестовий потік), щоб у `localStorage` з’явився `business` і всі сторінки dashboard були доступні.

---

## 9. Модалки та додаткові потоки (перевірено / виправлено)

| Що | Статус |
|----|--------|
| **Створення запису** (CreateAppointmentForm) | ✅ POST `/api/appointments`, перевірка конфліктів, нормалізація телефону, upsert клієнта |
| **Редагування запису** (EditAppointmentForm) | ✅ PATCH `/api/appointments/[id]` з businessId, видалення запису |
| **Графік майстра** (MasterScheduleModal) | ✅ PATCH `/api/masters/[id]` — workingHours, scheduleDateOverrides (JSON) |
| **Деталі клієнта / Історія** | Є на сторінці Клієнти (розгортання картки) та в StatisticsDetailModal |
| **Forgot password** `/forgot-password` | ✅ POST `/api/auth/forgot-password`; у dev показується посилання для скидання + кнопка «Копіювати» |
| **Reset password** `/reset-password?token=...` | ✅ GET перевірка токену, POST зміна пароля; заміна alert на toast; посилання «Відновити пароль знову» при невалідному токені |
| **Адмін-центр** `/admin/login` → `/admin/control-center` | ✅ Логін, Огляд, Бізнеси (таблиця, пошук, фільтри, Експорт CSV), модалка «Деталі» бізнесу, Телефонний довідник, Архів дій; toast замість alert для «Копіювати ID» |
| **Бронювання** `/booking/[slug]` | Завантаження бізнесу по slug через `/api/business/[param]` (slug підтримується) |
| **QR** `/qr/[slug]` | ✅ QR код + посилання; додано кнопку «Копіювати посилання» з toast |

---

## 10. Виправлення та покращення (впроваджено)

- **Reset password:** заміна `alert()` на toast; додано посилання «Відновити пароль знову» на екрані з невалідним токеном.
- **Forgot password (dev):** після успішної відправки показується посилання для скидання та кнопка «Копіювати» для зручного тесту без email.
- **QR сторінка:** кнопка «Копіювати посилання» з підтвердженням через toast.
- **База даних:** API appointments (POST/PATCH) коректно працює з полями customServiceName, customPrice, services (JSON), перевірка конфліктів часу, upsert клієнта по телефону.
- **Адмін-центр:** layout без захардкодженого `bg-[#0F172A]` — фон береться з теми (body); у модалці деталей бізнесу та в таблиці бізнесів кнопка «Копіювати ID» показує toast замість alert.

---

## 11. Адмін-центр (детальна перевірка)

| Вкладка / елемент | Статус |
|-------------------|--------|
| Логін `/admin/login` | ✅ Вхід за email/паролем, редірект на control-center |
| Огляд | ✅ Статистика (картки), Live-бар, кнопки Синхронізувати / Оновити |
| Бізнеси | ✅ Таблиця, пошук (за ID/назвою/email), фільтр статусу, Експорт CSV, пагінація |
| Кнопки в рядку: Деталі, Заблокувати, Видалити | ✅ Деталі — модалка з даними бізнесу та кнопками Заблокувати/Копіювати ID |
| Телефонний довідник | ✅ Таблиця номерів, категорія (Всі/Бізнес/Клієнт), пошук |
| Архів дій | ✅ Список подій (business_created, client_created, appointment_created), фільтр типу дії |
| Стиль | ✅ card-glass, modal-dialog; layout підлаштований під глобальні теми (світла/темна/OLED) |
