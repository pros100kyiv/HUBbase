# 🔗 Всі посилання застосунку

## 🏠 Головна сторінка
**http://localhost:3000**
- Кнопка "РЕЄСТРАЦІЯ БІЗНЕСУ" → `/register`
- Кнопка "ВХІД ДЛЯ БІЗНЕСУ" → `/login`
- Кнопка "🧪 Тестовий потік" → `/test-flow`
- Кнопка "📅 Приклад бронювання" → `/booking/045-barbershop`
- Кнопка "📱 Приклад QR" → `/qr/045-barbershop`

## 📝 Реєстрація
**http://localhost:3000/register**
- Форма реєстрації
- Кнопка "Зареєструватися" → зберігає в localStorage → `/dashboard`
- Посилання "Увійти" → `/login`

## 🔐 Вхід
**http://localhost:3000/login**
- Форма входу
- Кнопка "Увійти" → зберігає в localStorage → `/dashboard`
- Посилання "Зареєструватися" → `/register`

## 🧪 Тестовий потік (РЕКОМЕНДОВАНО ДЛЯ ТЕСТУВАННЯ)
**http://localhost:3000/test-flow**
- Кнопка "Увійти автоматично" → автоматичний вхід → `/dashboard`
- Всі швидкі посилання в одному місці
- Покрокова інструкція

## 📊 Dashboard (після входу)
**http://localhost:3000/dashboard**

### Кнопки:
- **"Копіювати"** → копіює посилання в буфер
- **"Відкрити"** → `/booking/[slug]`
- **"Відкрити QR"** → `/qr/[slug]`
- **"Нова вкладка"** → `/qr/[slug]` (нова вкладка)
- **"Відкрити налаштування"** → `/dashboard/settings`

### Навігація (Navbar):
- Назва бізнесу (клік) → `/dashboard`
- "Панель" → `/dashboard`
- "Налаштування" → `/dashboard/settings`
- "Сторінка бронювання" → `/booking/[slug]` (нова вкладка)
- "QR код" → `/qr/[slug]` (нова вкладка)
- "Вийти" → очищає localStorage → `/login`

## ⚙️ Налаштування (після входу)
**http://localhost:3000/dashboard/settings**

### Кнопки:
- **"← Назад до панелі"** → `/dashboard`
- **"Зберегти"** (в табі Інформація) → зберігає дані
- **"+ Додати майстра"** → показує форму
- **"Зберегти"** (форма майстра) → зберігає майстра
- **"Скасувати"** (форма майстра) → закриває форму
- **"Редагувати"** (майстер) → відкриває форму редагування
- **"Видалити"** (майстер) → видаляє майстра
- **"+ Додати послугу"** → показує форму
- **"Зберегти"** (форма послуги) → зберігає послугу
- **"Скасувати"** (форма послуги) → закриває форму
- **"Редагувати"** (послуга) → відкриває форму редагування
- **"Видалити"** (послуга) → видаляє послугу
- **"Зберегти кольори"** → зберігає кольори

### Таби:
- **"Інформація"** → основна інформація про бізнес
- **"Майстри"** → управління майстрами
- **"Послуги"** → управління послугами
- **"Кольори"** → налаштування кольорів

## 📅 Бронювання (публічна сторінка)
**http://localhost:3000/booking/[slug]**

### Крок 1 - Landing:
- Кнопка "ЗАБРОНЮВАТИ" → крок 2
- Кнопка "← Головна" → `/`

### Крок 2 - Послуги:
- Клік на послугу → додає/видаляє з вибраних
- Кнопка "Назад" → крок 1
- Кнопка "Далі" → крок 3 (якщо є вибрані послуги)

### Крок 3 - Майстер:
- Клік на майстра → вибирає майстра
- Кнопка "Назад" → крок 2
- Кнопка "Далі" → крок 4 (якщо вибрано майстра)

### Крок 4 - Час:
- Клік на дату → вибирає дату
- Клік на час → вибирає час
- Кнопка "Назад" → крок 3
- Кнопка "Далі" → крок 5 (якщо вибрано дату та час)

### Крок 5 - Контакти:
- Введення імені та телефону
- Кнопка "Назад" → крок 4
- Кнопка "Підтвердити бронювання" → створює запис → повідомлення → можливість створити ще або повернутися на головну

## 📱 QR код (публічна сторінка)
**http://localhost:3000/qr/[slug]**

### Кнопки:
- **"Відкрити бронювання"** → `/booking/[slug]`
- **"Назад"** → `/dashboard` (якщо залогінений) або `/` (якщо ні)

## 🎯 Повний тестовий потік

1. **Головна** (`/`) → "Тестовий потік"
2. **Тестовий потік** (`/test-flow`) → "Увійти автоматично"
3. **Dashboard** (`/dashboard`) → "Відкрити налаштування"
4. **Налаштування** (`/dashboard/settings`) → Додати майстра та послугу → "← Назад до панелі"
5. **Dashboard** → "Відкрити" (на картці Посилання)
6. **Бронювання** (`/booking/[slug]`) → Пройти всі 5 кроків
7. **Dashboard** → Перевірити запис в журналі
8. **Dashboard** → "Відкрити QR"
9. **QR код** (`/qr/[slug]`) → "Відкрити бронювання" або "Назад"

## ✅ Тестовий акаунт

- **Email:** admin@045barbershop.com
- **Password:** password123
- **Slug:** 045-barbershop

## 🔄 Логіка навігації

- Після реєстрації → автоматично `/dashboard`
- Після входу → автоматично `/dashboard`
- Після створення бронювання → можливість створити ще або `/`
- З Dashboard → можна перейти в налаштування, QR, бронювання
- З налаштувань → "← Назад до панелі" → Dashboard
- З QR → "Назад" → Dashboard або головна
- З бронювання → "← Головна" → головна сторінка

Всі кнопки працюють послідовно та логічно! 🎉




