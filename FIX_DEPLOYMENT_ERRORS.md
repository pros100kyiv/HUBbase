# Виправлення помилок деплою на Vercel

## ✅ Виправлено (2025-02-01):

1. **Помилка `pify(...).bind is not a function`**
   - **Причина:** `next-pwa` несумісний з Next.js 15+
   - **Рішення:** Видалено `next-pwa` з залежностей
   - **Статус:** ✅ Виправлено

2. **Несумісність версій `@next/swc`**
   - **Причина:** Версія `@next/swc` не відповідала версії Next.js
   - **Рішення:** Оновлено Next.js до версії 16.1.6
   - **Статус:** ✅ Виправлено

3. **Помилка завантаження `next.config.js`**
   - **Причина:** Конфлікт з `next-pwa`
   - **Рішення:** Видалено `next-pwa`, спрощено конфігурацію
   - **Статус:** ✅ Виправлено

## 🔴 Типові причини помилок деплою:

### 1. DATABASE_URL не налаштовано ❌

**Симптоми:**
- Деплой падає з помилкою "DATABASE_URL is not set"
- Помилки підключення до бази даних

**Рішення:**
1. Перейдіть в Vercel Dashboard → Settings → Environment Variables
2. Додайте:
   ```
   DATABASE_URL=postgresql://neondb_owner:npg_9VohXeW6nPIM@ep-cold-bar-agx5y4cp-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```
3. Перезберіть проект

### 2. Prisma Client не генерується ❌

**Симптоми:**
- Помилка: "Prisma Client not generated"
- Помилка: "Cannot find module '@prisma/client'"

**Рішення:**
Перевірте, чи в `package.json` є:
- `"build": "prisma generate && next build"` ✅ (вже є)
- `"postinstall": "prisma generate"` ✅ (вже є)

### 3. Помилки збірки ❌

**Симптоми:**
- Build fails з помилками TypeScript/ESLint
- Помилки імпортів

**Рішення:**
Перевірте логи в Vercel Dashboard → Deployments → останній деплой → Logs

### 4. Відсутні змінні оточення ❌

**Симптоми:**
- Помилки при виконанні API routes
- Помилки автентифікації

**Рішення:**
Додайте всі необхідні змінні в Vercel:
- `DATABASE_URL` - обов'язково
- `GOOGLE_CLIENT_ID` - опціонально
- `GOOGLE_CLIENT_SECRET` - опціонально

## 📋 Чеклист для виправлення:

### Крок 1: Перевірте Environment Variables на Vercel

1. Vercel Dashboard → ваш проект → Settings → Environment Variables
2. Переконайтеся, що є:
   ```
   DATABASE_URL=postgresql://neondb_owner:npg_9VohXeW6nPIM@ep-cold-bar-agx5y4cp-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```

### Крок 2: Перевірте Build Settings

1. Settings → General → Build & Development Settings
2. Переконайтеся:
   - Build Command: `npm run build` ✅
   - Install Command: `npm install` ✅
   - Output Directory: `.next` ✅

### Крок 3: Перевірте логи деплою

1. Deployments → останній деплой → Logs
2. Шукайте помилки з ключовими словами:
   - "DATABASE_URL"
   - "Prisma"
   - "Error"
   - "Failed"

### Крок 4: Перезберіть проект

Після додавання змінних:
1. Deployments → три крапки → Redeploy
2. Або зробіть новий commit і push

## 🔍 Як перевірити помилки:

### В Vercel Dashboard:
1. Відкрийте проект
2. Перейдіть в Deployments
3. Виберіть невдалий деплой (червоний X)
4. Натисніть "View Function Logs" або "View Build Logs"
5. Шукайте помилки

### Через GitHub:
1. Відкрийте репозиторій на GitHub
2. Перейдіть в Actions (якщо налаштовано)
3. Перевірте логи невдалого workflow

## ✅ Після виправлення:

1. Переконайтеся, що всі змінні додані
2. Зробіть redeploy або новий commit
3. Перевірте, чи деплой успішний (зелена галочка)
4. Відкрийте сайт і перевірте роботу

## 🆘 Якщо нічого не допомагає:

1. Перевірте, чи локально проект збирається:
   ```bash
   npm run build
   ```

2. Перевірте підключення до бази даних:
   ```bash
   npm run db:check
   ```

3. Перевірте логи на Vercel детально

4. Спробуйте створити новий проект на Vercel і підключити репозиторій заново


