# Система управління бізнесами через Telegram бота

## 🎯 Огляд системи

Повноцінна система управління бізнесами та адміністраторами через Telegram бота з інтеграцією Neon PostgreSQL та детальною аналітикою прибутку.

## ✨ Основні можливості

### 1. Реєстрація бізнесів
- ✅ Модуль реєстрації з вибором сфери діяльності
- ✅ Підтримка ніш: SALON, BARBERSHOP, STO, CAR_WASH, SPA, FITNESS, OTHER
- ✅ Індивідуальна персоналізація для кожного бізнесу
- ✅ Налаштування розсилок та статистики

### 2. Управління користувачами (100-200 користувачів)
- ✅ Система ролей: OWNER, ADMIN, MANAGER, EMPLOYEE, VIEWER
- ✅ Детальні права доступу для кожної ролі
- ✅ Управління через API та Telegram інтерфейс
- ✅ Логування всіх дій

### 3. Аналітика прибутку та прогнозування
- ✅ **Поточний прибуток**: тільки з завершених візитів (status = 'Done')
- ✅ **Прогнозований прибуток**: на основі підтверджених візитів (status = 'Confirmed')
- ✅ Аналітика по типах послуг та їх маржинальності
- ✅ Ефективність рекламних кампаній
- ✅ Тренди та порівняння з попередніми періодами

### 4. Моніторинг та сповіщення
- ✅ Автоматичні сповіщення про критичні зміни прибутку
- ✅ Попередження про відхилення від плану
- ✅ Відстеження трендів та прогнозів
- ✅ Постійний моніторинг показників

## 📋 Структура системи

### API Endpoints

#### Реєстрація та налаштування
- `POST /api/telegram/business-register` - Реєстрація нового бізнесу
- `POST /api/telegram/setup` - Налаштування Telegram бота для бізнесу
- `POST /api/telegram/webhook?businessId=xxx` - Webhook для отримання оновлень

#### Управління користувачами
- `POST /api/telegram/register` - Реєстрація користувача в Telegram боті
- `GET /api/telegram/users?businessId=xxx` - Список користувачів
- `PATCH /api/telegram/users` - Оновлення користувача (роль, права)

#### Сповіщення
- `POST /api/telegram/send-notification` - Відправка сповіщення
- `POST /api/telegram/integrate-alerts` - Інтеграція сповіщень з аналітики

### Команди Telegram бота

#### Для всіх користувачів
- `/start` - Реєстрація/вхід в систему
- `/help` - Список доступних команд

#### Статистика (VIEWER+)
- `/stats` - Загальна статистика за місяць

#### Аналітика (MANAGER+)
- `/revenue` - Детальна аналітика прибутку з трендами
- `/alerts` - Активні сповіщення про зміни

## 🔐 Система ролей та прав

| Роль | Права доступу |
|------|---------------|
| **OWNER** | Всі права: статистика, аналітика, сповіщення, управління користувачами, налаштування |
| **ADMIN** | Статистика, аналітика, сповіщення, управління користувачами |
| **MANAGER** | Статистика, аналітика, сповіщення |
| **EMPLOYEE** | Статистика |
| **VIEWER** | Немає доступу |

## 📊 Аналітика прибутку

### Розрахунок поточного прибутку
- Базується **тільки** на завершених візитах (status = 'Done')
- Враховує `customPrice` (індивідуальні ціни)
- Розподіляє прибуток по послугах

### Прогнозований прибуток
- Базується на підтверджених візитах (status = 'Confirmed')
- Дозволяє планувати майбутній дохід
- Порівнюється з поточним прибутком

### Аналітика по послугах
- Прибуток від кожної послуги
- Кількість візитів
- Середній чек
- Маржинальність (якщо додано вартість)

### Ефективність рекламних кампаній
- Відстеження джерел записів (`source`, `campaignId`)
- Конверсія кампаній
- ROI (якщо додано вартість кампаній)

## 🔔 Система сповіщень

### Типи сповіщень
- **Критичні** (🔴): Падіння прибутку > 20%
- **Попередження** (🟡): Зниження прибутку > 10%
- **Інформаційні** (🔵): Зростання прибутку > 20%

### Автоматичні перевірки
- Зміна поточного прибутку порівняно з попереднім періодом
- Зміна прогнозованого прибутку
- Кількість підтверджених візитів
- Відхилення від плану

### Моніторинг
Скрипт `scripts/telegram-monitor.ts` автоматично:
- Перевіряє сповіщення для всіх активних бізнесів
- Відправляє критичні та попередження в Telegram
- Може запускатися через cron

## 🚀 Швидкий старт

### 1. Встановлення
```bash
npm install
```

### 2. Створення Telegram бота
1. Відкрийте [@BotFather](https://t.me/botfather)
2. Відправте `/newbot`
3. Скопіюйте токен

### 3. Реєстрація бізнесу
```bash
POST /api/telegram/business-register
{
  "name": "Мій бізнес",
  "email": "business@example.com",
  "phone": "+380123456789",
  "niche": "SALON",
  "telegramBotToken": "xxx:xxx",
  "password": "secure_password"
}
```

### 4. Налаштування webhook
```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-domain.com/api/telegram/webhook?businessId=<BUSINESS_ID>"
```

### 5. Запуск моніторингу
```bash
npm run telegram:monitor
```

Або через cron (кожні 30 хвилин):
```bash
*/30 * * * * cd /path/to/project && npm run telegram:monitor
```

## 📁 Структура файлів

```
lib/
  telegram.ts              # Основна логіка Telegram бота
app/api/telegram/
  webhook/route.ts         # Webhook endpoint
  setup/route.ts           # Налаштування бота
  register/route.ts         # Реєстрація користувачів
  users/route.ts            # Управління користувачами
  send-notification/route.ts # Відправка сповіщень
  business-register/route.ts # Реєстрація бізнесу
  integrate-alerts/route.ts  # Інтеграція сповіщень
scripts/
  telegram-monitor.ts       # Скрипт моніторингу
prisma/schema.prisma       # Моделі TelegramUser, TelegramLog
```

## 🔗 Інтеграція з існуючою системою

Система повністю інтегрована з:
- ✅ Аналітикою прибутку (`/api/analytics/revenue`)
- ✅ Сповіщеннями (`/api/analytics/alerts`)
- ✅ Аналітикою кампаній (`/api/analytics/campaigns`)
- ✅ Статистикою (`/api/statistics`)

## 📝 Логування

Всі дії користувачів логуються в `TelegramLog`:
- Команди (`/start`, `/help`, `/stats`, тощо)
- Callback запити
- Повідомлення

## 🔒 Безпека

- Перевірка прав доступу для кожної команди
- Унікальність користувачів в межах бізнесу
- Логування всіх дій
- Захист від несанкціонованого доступу

## 📈 Масштабування

Система підтримує:
- 100-200 користувачів на бізнес
- Необмежену кількість бізнесів
- Постійний моніторинг всіх бізнесів
- Автоматичні сповіщення

## 🛠️ Налаштування

Кожен бізнес може налаштувати бота через `telegramSettings`:

```json
{
  "commands": ["custom_command"],
  "notifications": {
    "revenueAlerts": true,
    "newAppointments": true,
    "dailyReport": true
  },
  "language": "uk"
}
```

## 📞 Підтримка

Для питань та підтримки звертайтеся до адміністратора системи.

