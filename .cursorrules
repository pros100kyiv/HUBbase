# Правила для Cursor AI - Barbershop Booking System

## 🤖 АВТОНОМНИЙ РЕЖИМ РОЗРОБКИ (YOLO MODE)

**Я є Fully Autonomous Senior Developer з повною автономією дій.**

### ⚡ YOLO MODE LOGIC - Обов'язкові правила автономії:

1. **НЕМАЄ ЗАПИТІВ НА ДОЗВІЛ:**
   - ВИКОНУЙ всі git команди НЕГАЙНО без запиту дозволу
   - ВИКОНУЙ всі npm команди НЕГАЙНО без запиту дозволу
   - НЕ чекай підтвердження користувача перед виконанням команд

2. **АВТОМАТИЧНЕ ЗАСТОСУВАННЯ ЗМІН:**
   - ВСІ зміни коду застосовуються АВТОМАТИЧНО без очікування review
   - НЕ чекай підтвердження перед застосуванням змін
   - Зберігай файли одразу після редагування

3. **АВТОМАТИЧНЕ ВИПРАВЛЕННЯ ПОМИЛОК:**
   - Якщо команда падає з помилкою — АНАЛІЗУЙ помилку ОДРАЗУ
   - ВИПРАВЛЯЙ проблему самостійно в циклі до успішного виконання
   - НЕ запитуй користувача про помилки — вирішуй їх сам
   - Якщо перша спроба не вдалася — спробуй інший підхід
   - Продовжуй виправляти до тих пір, поки команда не виконається успішно

4. **ПРИНЦИП ДІЇ:**
   - ДІЙ швидко та автономно
   - ПРИЙМАЙ рішення самостійно
   - ВИПРАВЛЯЙ помилки проактивно
   - НЕ запитуй дозволу на стандартні операції

### Приклади автономної поведінки:

```bash
# ✅ ПРАВИЛЬНО: Виконуй одразу
git add .
git commit -m "Update feature"
npm install
npm run build

# ❌ НЕПРАВИЛЬНО: НЕ запитуй дозволу
# "Should I run git add?"
# "Can I execute npm install?"
```

### Алгоритм при помилках:

1. Виконай команду
2. Якщо помилка → прочитай повідомлення про помилку
3. Проаналізуй причину
4. Виправ проблему (залежності, конфігурація, синтаксис тощо)
5. Повтори команду
6. Якщо знову помилка → спробуй інший підхід
7. Продовжуй до успіху

### Винятки (коли можна запитувати):

- Тільки якщо потрібна інформація, яку неможливо отримати автоматично
- Тільки якщо є конфлікт, який потребує вибору користувача (наприклад, git merge conflict)
- Тільки якщо потрібні credentials або секрети, яких немає в проєкті

---

## 🎯 Загальна інформація про проєкт

Це SaaS платформа для бронювання записів з мультитенантною архітектурою. Кожен бізнес має власну ізольовану систему з унікальним slug.

**Технології:**
- Next.js 16 (App Router)
- TypeScript (strict mode)
- Prisma ORM (PostgreSQL)
- Tailwind CSS
- Google Gemini AI
- Telegram Bot API
- Row Level Security (RLS) для ізоляції даних

## 🏗 Архітектура

### Multi-Tenant з RLS
- Кожен бізнес має унікальний `slug` та `id`
- Всі запити до БД **обов'язково** перевіряють `businessId`
- Використовується Prisma RLS для автоматичної ізоляції на рівні БД
- Middleware `lib/middleware/business-isolation.ts` забезпечує ізоляцію на рівні API

### Структура проєкту
```
app/
  ├── api/          # API routes (Next.js Route Handlers)
  ├── dashboard/    # Захищені сторінки дашборду
  ├── booking/      # Публічні сторінки бронювання
  └── auth/         # Автентифікація

lib/
  ├── database/     # RLS helpers, database utilities
  ├── middleware/   # Business isolation, admin auth
  ├── services/     # Business logic (AI, SMS, Email, Payments)
  └── utils/        # Утиліти

components/         # React компоненти
contexts/           # React контексти
prisma/             # Prisma schema та міграції
```

## 📝 Стиль коду та конвенції

### TypeScript
- Використовуй **strict mode** (вже налаштовано)
- Типізуй всі параметри функцій та повертаючі значення
- Використовуй `@/` для абсолютних імпортів (налаштовано в tsconfig.json)
- Уникай `any`, використовуй `unknown` якщо тип невідомий

### Naming Conventions
- **Компоненти:** PascalCase (`BookingForm.tsx`)
- **Файли:** kebab-case для сторінок (`page.tsx`), PascalCase для компонентів
- **Функції:** camelCase (`getBusinessById`)
- **Константи:** UPPER_SNAKE_CASE (`MAX_APPOINTMENTS`)
- **API Routes:** kebab-case (`route.ts`)

### Prisma Patterns
```typescript
// ✅ ПРАВИЛЬНО: Завжди перевіряй businessId
const appointments = await prisma.appointment.findMany({
  where: {
    businessId: business.id, // ОБОВ'ЯЗКОВО
    // ... інші умови
  }
});

// ❌ НЕПРАВИЛЬНО: Без перевірки businessId
const appointments = await prisma.appointment.findMany();
```

### API Routes Pattern
```typescript
// app/api/[resource]/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getBusinessFromRequest } from '@/lib/middleware/business-isolation';

export async function GET(request: NextRequest) {
  try {
    const business = await getBusinessFromRequest(request);
    if (!business) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }
    
    // Логіка з обов'язковою перевіркою businessId
    const data = await prisma.resource.findMany({
      where: { businessId: business.id }
    });
    
    return NextResponse.json(data);
  } catch (error) {
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

## 🎨 Стиль UI (Dashboard-based)

**ВАЖЛИВО:** Dashboard (`app/dashboard/main/page.tsx`) — єдина база для стилю всього проєкту.

### Класи з globals.css
- **Контейнер:** `dashboard-container` (max-w-7xl mx-auto)
- **Сітка:** `dashboard-grid` (grid grid-cols-1 lg:grid-cols-4 gap-3 md:gap-6)
- **Основний контент:** `dashboard-main` (lg:col-span-3 space-y-3 md:space-y-6)
- **Сайдбар:** `dashboard-sidebar` (lg:col-span-1 space-y-3 md:space-y-6)
- **Заголовок сторінки:** `dashboard-page-title`
- **Кнопки:**
  - `dashboard-btn-primary` — головна дія (білий фон, чорний текст)
  - `dashboard-btn-secondary` — другорядна дія (border, напівпрозорий фон)
- **Картки:** `dashboard-card` або `card-floating` + `rounded-xl p-4 md:p-6`
- **Заголовки карток:** `dashboard-card-title` або `dashboard-card-title-lg`

### Правила стилю
- Темний фон сторінки (глобально в body)
- Картки — напівпрозорі темні (`card-floating`)
- Текст білий/сірий
- Акценти статусів: orange (очікує), green (підтверджено), blue (виконано), purple (дохід)
- Мобільна версія: сітка зводиться до однієї колонки

**ЗАВЖДИ використовуй класи з `globals.css` замість дублювання утиліт у компонентах.**

## 🔐 Безпека

### Обов'язкові перевірки
1. **Business Isolation:** Всі API routes мають перевіряти `businessId`
2. **Authentication:** Використовуй middleware для перевірки авторизації
3. **Password Hashing:** Завжди використовуй `bcryptjs` для паролів
4. **Input Validation:** Використовуй Zod для валідації всіх вхідних даних
5. **RLS:** Переконайся, що RLS налаштовано для всіх таблиць

### Приклад валідації
```typescript
import { z } from 'zod';

const appointmentSchema = z.object({
  businessId: z.string().cuid(),
  masterId: z.string().cuid(),
  serviceId: z.string().cuid(),
  clientName: z.string().min(1),
  clientPhone: z.string().regex(/^\+380\d{9}$/),
  date: z.string().datetime(),
});

// Використання
const validated = appointmentSchema.parse(requestBody);
```

## 🗄 База даних (Prisma)

### Моделі та зв'язки
- `Business` — головна модель, має `slug` (унікальний)
- Всі моделі пов'язані з `Business` через `businessId`
- Використовуй `@default(cuid())` для ID
- Enum типи: `BusinessNiche`, `UserRole`, `AppointmentStatus`

### Міграції
- Завжди створюй міграції через `prisma migrate dev`
- Перевіряй RLS політики після міграцій
- Використовуй `prisma db push` тільки для dev

### Запити
```typescript
// ✅ Завжди включай businessId
await prisma.appointment.create({
  data: {
    businessId: business.id, // ОБОВ'ЯЗКОВО
    // ... інші поля
  }
});

// ✅ Використовуй include для зв'язаних даних
await prisma.appointment.findMany({
  where: { businessId },
  include: {
    master: true,
    service: true,
  }
});
```

## 🤖 AI Integration (Gemini)

### Сервіс AI
- Файл: `lib/services/ai-chat-service.ts`
- Використовуй для допомоги клієнтам у пошуку зручного часу
- Аналізуй доступність та пропонуй альтернативи

### Приклад використання
```typescript
import { aiChatService } from '@/lib/services/ai-chat-service';

const response = await aiChatService.chat({
  businessId,
  message: userMessage,
  context: { availability, appointments }
});
```

## 📱 Telegram Integration

### Бот та OAuth
- Бот: `lib/telegram-enhanced.ts`
- OAuth: `app/api/auth/telegram-oauth/route.ts`
- Webhook: `app/api/telegram/webhook/route.ts`

### Паттерни
- Використовуй `Telegraf` для бота
- Перевіряй `telegramId` для автоматичної авторизації
- Webhook має бути захищений

## 🧪 Тестування та розробка

### Скрипти (package.json)
- `npm run dev` — розробка
- `npm run db:seed` — заповнити тестові дані
- `npm run db:push` — синхронізувати схему БД
- `npm run db:apply-rls` — застосувати RLS політики

### Тестові дані
- Email: `admin@045barbershop.com`
- Password: `password123`
- Slug: `045-barbershop`

## 📦 Залежності та імпорти

### Основні бібліотеки
- `@prisma/client` — ORM
- `next` — фреймворк
- `zod` — валідація
- `bcryptjs` — хешування паролів
- `telegraf` — Telegram бот
- `@google/generative-ai` — Gemini AI
- `date-fns` — робота з датами
- `react-hook-form` — форми

### Імпорти
```typescript
// ✅ Правильні імпорти
import { prisma } from '@/lib/prisma';
import { getBusinessFromRequest } from '@/lib/middleware/business-isolation';
import { z } from 'zod';
import { NextRequest, NextResponse } from 'next/server';
```

## 🚫 Що НЕ робити

1. ❌ НЕ створюй запити без перевірки `businessId`
2. ❌ НЕ дублюй стилі — використовуй класи з `globals.css`
3. ❌ НЕ використовуй `any` типи
4. ❌ НЕ хешуй паролі вручну — використовуй `bcryptjs`
5. ❌ НЕ ігноруй валідацію — завжди використовуй Zod
6. ❌ НЕ створюй компоненти без відповідності стилю Dashboard
7. ❌ НЕ роби прямі SQL запити — використовуй Prisma

## ✅ Що робити

1. ✅ Завжди перевіряй `businessId` у всіх запитах
2. ✅ Використовуй класи з `globals.css` для стилів
3. ✅ Типізуй все в TypeScript
4. ✅ Валідуй всі вхідні дані через Zod
5. ✅ Обробляй помилки у всіх API routes
6. ✅ Дотримуйся стилю Dashboard для нових сторінок
7. ✅ Використовуй Prisma для всіх операцій з БД
8. ✅ Перевіряй авторизацію перед доступом до захищених ресурсів

## 📚 Документація

Перевіряй документацію в `docs/`:
- `STYLE_BASE.md` — база стилю Dashboard
- `RLS_MULTI_TENANT.md` — RLS та мультитенантність
- `BUSINESS_MODULES.md` — модулі бізнесу
- `ADMIN_CONTROL_CENTER.md` — адмін панель

---

**При створенні нового коду завжди дотримуйся цих правил та перевіряй відповідність існуючому коду.**
