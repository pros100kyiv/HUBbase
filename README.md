# Xbase — платформа записів та клієнтів (Barbershop Booking System)

Production-ready SaaS платформа для записів та клієнтів. Кожен бізнес реєструється і отримує власну базу записів з унікальним URL.

## 🚀 Основні можливості

### Для бізнесів:
- ✅ Реєстрація та автентифікація
- ✅ Dashboard з щоденним журналом записів
- ✅ Унікальний URL для клієнтів: `/booking/[slug]`
- ✅ Налаштування брендингу (кольори, назва, опис)
- ✅ Управління майстрами та послугами
- ✅ QR код генератор
- ✅ AI помічник для допомоги клієнтам (LM Studio)

### Для клієнтів:
- ✅ 5-кроковий майстер бронювання
- ✅ Вибір послуг, майстра, дати та часу
- ✅ Валідація даних (телефон +380)
- ✅ Перевірка конфліктів записів
- ✅ AI помічник для пошуку зручного часу

## 🎨 Стиль і макет

**Dashboard — база для всього UI.** Сітка, заголовки, картки та кнопки всіх сторінок дашборду (Записи, Клієнти, Спеціалісти тощо) мають відповідати головній сторінці Dashboard. Детально: [docs/STYLE_BASE.md](docs/STYLE_BASE.md).

## 📤 Git

**Після кожного коміту** зміни автоматично відправляються на GitHub (хук `post-commit`). Достатньо виконувати `git add` та `git commit` — пуш відбудеться сам.

## 🛠 Технології

- **Next.js 14** (App Router)
- **TypeScript**
- **Prisma** (SQLite для dev, PostgreSQL для prod)
- **Tailwind CSS**
- **LM Studio** (локальний LLM)
- **bcryptjs** (хешування паролів)
- **PWA Support**

## 📦 Встановлення

1. **Клонуйте репозиторій та встановіть залежності:**
```bash
npm install
```

2. **Налаштуйте базу даних:**
```bash
# Створіть .env файл
echo 'DATABASE_URL="file:./dev.db"' > .env

# Створіть базу даних
npx prisma generate
npx prisma db push
```

3. **Заповніть тестові дані:**
```bash
npm run db:seed
```

4. **Запустіть сервер розробки:**
```bash
npm run dev
```

## 🧪 Тестування

**E2E (Playwright):** `npm run test:e2e` — 47 тестів у браузері. Швидкий smoke: `npm run test:e2e:critical`. Детально: [e2e/README.md](e2e/README.md).

**API:** `npm run test:api` — потрібен запущений `npm run dev`.

## 🧪 Тестовий акаунт

Після seed:
- **Email:** admin@045barbershop.com
- **Password:** password123
- **Slug:** 045-barbershop

## 📍 Маршрути

### Публічні:
- `/register` - Реєстрація бізнесу
- `/login` - Вхід для бізнесу
- `/booking/[slug]` - Сторінка бронювання для клієнтів
- `/qr/[slug]` - QR код для бронювання

### Захищені (потребують авторизації):
- `/dashboard` - Панель управління бізнесу
- `/dashboard/settings` - Налаштування бізнесу

## 🗄 Структура бази даних

```
Business (бізнес)
├── Masters (майстри)
├── Services (послуги)
└── Appointments (записи)
```

Кожен бізнес має:
- Унікальний `slug` для URL
- Власні майстри, послуги та записи
- Налаштування брендингу (кольори, назва)

## 🔐 Безпека

- Паролі хешуються через bcrypt
- Ізоляція даних між бізнесами (всі запити перевіряють businessId)
- Валідація через Zod

## 🤖 AI Помічник (LM Studio)

AI помічник допомагає клієнтам знайти зручний час:
- Аналізує доступність
- Пропонує альтернативні дати
- Розуміє природну мову ("ранок", "завтра", тощо)

**Налаштування:** Запустіть LM Studio локально та додайте URL та модель в Центрі управління (`/admin/control-center`).

## 📱 PWA

Система підтримує PWA:
- Офлайн режим
- Встановлення на пристрій
- Швидкий доступ

## 🚀 Production Deployment

1. **Змініть базу даних на PostgreSQL:**
```env
DATABASE_URL="postgresql://user:password@host:5432/dbname"
```

2. **Додайте змінні оточення:**
```env
GEMINI_API_KEY=your_key
NEXT_PUBLIC_APP_URL=https://yourdomain.com
```

3. **Зберіть проект:**
```bash
npm run build
npm start
```

## 📝 API Endpoints

### Публічні:
- `GET /api/business/[slug]` - Отримати інформацію про бізнес
- `GET /api/services?businessId=...` - Послуги бізнесу
- `GET /api/masters?businessId=...` - Майстри бізнесу
- `GET /api/availability?businessId=...&masterId=...&date=...` - Доступність
- `POST /api/appointments` - Створити запис

### Захищені:
- `POST /api/auth/register` - Реєстрація
- `POST /api/auth/login` - Вхід
- `PATCH /api/business/[id]` - Оновити налаштування

## 🎨 Налаштування брендингу

Кожен бізнес може налаштувати:
- Назву та опис
- Кольори (primary, background, surface)
- Логотип (майбутнє)

## 📄 Ліцензія

MIT

## 🤝 Підтримка

Для питань та пропозицій створюйте Issues в репозиторії.
